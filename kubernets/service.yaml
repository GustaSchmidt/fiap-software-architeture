apiVersion: v1
kind: Service
metadata:
  name: fiap-app-service
  labels:
    app: fiap-app
spec:
  type: LoadBalancer
  selector:
    app: fiap-app
  ports:
  - protocol: TCP
    port: 80 
    targetPort: 8000